<div class="row">
	<div class="col-lg-3">
		<%= render 'shared/categories' %>
	</div>
	<div class="col-lg-9">
	<ol class="breadcrumb">
		<% if params[:id] %>
			<li><%= "Home > #{@sub_category.parent.title} > #{@sub_category.title}" %></li>
		<% else %>
			<li class="active">Home</li>
			<% end %>
	</ol>
		<%= render 'shared/products' %>
	</div>
</div>




